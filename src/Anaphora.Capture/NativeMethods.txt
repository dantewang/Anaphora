// CsWin32 input. One Win32 API, constant, or type name per line; the source
// generator emits bindings for exactly what is listed here and nothing else.
// Names must match the Win32 metadata exactly, so add entries as they are
// needed rather than speculatively -- an unknown name is a build warning.
